openapi: 3.0.0
info:
  title: an allOf of two plain scalar members vanishes
  version: "1.0"
paths:
  /actions:
    get:
      operationId: getActions
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  region_slug:
                    allOf:
                      - $ref: "#/components/schemas/Slug"
                      - type: string
                        nullable: true
components:
  schemas:
    Slug:
      type: string
