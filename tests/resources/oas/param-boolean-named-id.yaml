openapi: 3.0.3
info:
  title: a boolean parameter or field whose name ends in Id stays Boolean, never ID
  version: '1'
paths:
  /widgets:
    get:
      operationId: getWidgets
      parameters:
        - name: useGroupId
          in: query
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  archivedId:
                    type: boolean
