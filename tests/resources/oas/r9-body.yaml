openapi: 3.0.0
info:
  title: computed / literal request bodies (R9)
  version: 1.0.0
servers:
  - url: https://example.com
paths:
  /things:
    post:
      operationId: createThing
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                kind:
                  type: string
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
